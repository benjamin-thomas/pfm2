# Render.com blueprint for PFM2
# https://render.com/docs/blueprint-spec

services:
  # Backend API
  - type: web
    name: pfm2-api
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run build:server:prod
    startCommand: node dist/server/main.js
    envVars:
      - key: BE_HOST
        value: 0.0.0.0
      - key: FE_BASE_URL
        value: https://pfm2.onrender.com/health
      - key: REPO
        value: sql
      - key: NODE_ENV
        value: production

  # Frontend static site
  - type: web
    name: pfm2
    runtime: static
    buildCommand: npm ci --include=dev && npm run build:client
    staticPublishPath: dist/client
    envVars:
      - key: VITE_API_URL
        value: https://pfm2-api.onrender.com/health
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
    routes:
      - type: rewrite
        source: /*
        destination: /index.html